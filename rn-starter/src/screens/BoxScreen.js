import React from 'react';
import {Text, View, StyleSheet } from 'react-native';

const BoxScreen = () => {
    return ( <View>
        <Text>This is Box Screen Layout</Text>
    </View>
    )
};

const style = StyleSheet.create({});

export default BoxScreen;